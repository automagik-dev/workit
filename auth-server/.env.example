# Workit OAuth callback server configuration.
# Copy this file to .env and load it via your process manager.

# Server location and binary path.
WK_AUTH_SERVER_HOME=/opt/wk-auth-server
WK_AUTH_SERVER_BIN=/opt/wk-auth-server/auth-server

# Listener configuration.
WK_AUTH_SERVER_PORT=8089

# Redirect URL configured in Google Cloud Console.
# Must match the OAuth client callback exactly.
WK_REDIRECT_URL=https://auth.namastex.io/callback

# Credential source (choose one or set both):
# 1) Preferred: path to OAuth credentials JSON (web or installed format).
WK_CREDENTIALS_FILE=/etc/wk-auth-server/credentials.json
# 2) Direct credentials (used when file is empty or missing fields).
WK_CLIENT_ID=
WK_CLIENT_SECRET=

# If both a file and direct values are set, direct values take precedence.
