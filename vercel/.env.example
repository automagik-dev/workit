# OAuth credentials (from GCP console — Web Application type)
WK_CLIENT_ID=
WK_CLIENT_SECRET=
WK_REDIRECT_URL=https://auth.automagik.dev/callback

# Vercel KV — auto-configured by `vercel link` + `vercel env pull`
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# KV_REST_API_READ_ONLY_TOKEN=
